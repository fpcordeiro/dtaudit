% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-tables.R
\name{export_coef_table}
\alias{export_coef_table}
\title{Export Coefficient Table from Models}
\usage{
export_coef_table(models, models_names = NULL)
}
\arguments{
\item{models}{A list of fixest model objects.}

\item{models_names}{Optional character vector of model names (currently unused).}
}
\value{
A data.table with formatted regression output.
}
\description{
Creates a publication-ready coefficient table from fixest regression models
using modelsummary. Organizes output with sections for Variables, Fit
Statistics, and Fixed Effects.
}
\note{
Requires the \pkg{modelsummary} and \pkg{fixest} packages.
}
\examples{
\dontrun{
library(fixest)
library(data.table)
data(mtcars)
m1 <- feols(mpg ~ hp, data = mtcars)
m2 <- feols(mpg ~ hp + wt, data = mtcars)
export_coef_table(list(m1, m2))
}

}
